# Base URL of the ModernWeb CMS API, including the /api/v1 prefix.
# This origin must be listed in the API's ADMIN_ORIGINS, or the browser will
# refuse to send the refresh cookie and every reload lands on /login.
NEXT_PUBLIC_API_URL="http://localhost:3001/api/v1"
